"""
    This package contains the implementation of the inference module
"""

# Import builder class (builder_class) and inference metadata (inference_metadata) here

# Example:
# from .yolov8 import builder_class, inference_metadata

from .zip_model import ModelBuilderImplementation as model_builder_class, inference_metadata